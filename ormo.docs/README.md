# Ormo Documentation

The documentation site for Ormo. It consumes the local `@ormo/primitives` workspace package so documentation and component changes can be developed together.

## Development

From the repository root:

```sh
pnpm install
pnpm docs:dev
```

Or from this directory:

```sh
pnpm dev
```
