---
import type { AlertDialogActionProps } from "./types";
import Button from "../button/Button.astro";

type Props = AlertDialogActionProps;

const attributes: Props = Astro.props;
---

<Button {...attributes} data-ormo-alert-dialog-action>
  <slot />
</Button>
