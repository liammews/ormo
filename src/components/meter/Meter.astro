---
import type { MeterProps } from "./types";

type Props = MeterProps;

const { "data-ormo-meter": ormoMeterAttribute, ...attributes }: Props =
  Astro.props;
void ormoMeterAttribute;
---

<meter {...attributes} data-ormo-meter><slot /></meter>
