---
import type { DialogDescriptionProps } from "./types";

type Props = DialogDescriptionProps;

const { as: Tag = "p", ...attributes }: Props = Astro.props;
---

<Tag {...attributes} data-ormo-dialog-description>
  <slot />
</Tag>
