---
import type { AccordionHeaderProps } from "./types";

type Props = AccordionHeaderProps;

const { as: Tag = "h3", ...attributes }: Props = Astro.props;
---

<Tag {...attributes} data-ormo-accordion-header>
  <slot />
</Tag>
