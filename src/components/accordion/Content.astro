---
import type { AccordionContentProps } from "./types";

type Props = AccordionContentProps;

const { hiddenUntilFound = false, ...attributes } = Astro.props;
---

<div
  {...attributes}
  data-ormo-accordion-content
  data-state="closed"
  aria-hidden="true"
  inert
  hidden={hiddenUntilFound ? "until-found" : true}
  data-hidden-until-found={hiddenUntilFound ? "" : undefined}
>
  <slot />
</div>
