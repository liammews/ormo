---
"@ormo/primitives": minor
---

Add a custom-by-default Select primitive with an operating-system `native` mode, native form fallback, accessible groups, separators, clearing, typeahead, browser control, and optional Floating UI positioning.
