---
"@ormo/primitives": minor
---

Add an accessible Tooltip primitive for hover and focus descriptions, with Popover API top-layer rendering, CSS Anchor Positioning by default, optional Floating UI placement, composable parts, demos, and documentation.
