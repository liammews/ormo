---
"@ormo/primitives": minor
---

Add Checkbox, CheckboxIndicator, and CheckboxGroup primitives.

Ship a native checkbox with an optional indicator, and a role=group custom element for shared name, parent select-all, aggregate state, and at-least-one validation. Field can own a checkbox group for description and error wiring.
