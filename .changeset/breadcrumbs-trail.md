---
"@ormo/primitives": minor
---

Add the Breadcrumbs primitive.

Ship a zero-JavaScript `nav` / `ol` trail with `Root`, `List`, `Item`, `Link`, `Page`, and `Separator`, plus an opt-in Schema.org `BreadcrumbList` microdata mode.
